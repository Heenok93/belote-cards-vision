services:
  belote-app:
    build: .
    container_name: belote-card-vision
    ports:
      - "8501:8501"
    volumes:
      - ./models:/app/models:ro
    environment:
      - STREAMLIT_SERVER_ADDRESS=0.0.0.0
      - STREAMLIT_SERVER_PORT=8501
    restart: unless-stopped